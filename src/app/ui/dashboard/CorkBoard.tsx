import React from "react";
import styles from '@/app/dashboard/dashboard.module.scss';


export default function CorkBoard() {
    return (
        <div className={styles.corkContainer}>hello</div>

    );
}